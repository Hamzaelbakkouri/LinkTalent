export type AssignPlayer = {
    id: object;
    contractFile: string[];
    endDate: Date;
    leaderAssigned: object;
}

export type AssignPlayerRequest = {
    id: object;
    contractFile: string[];
    endDate: Date;
    leaderAssigned: object;
}